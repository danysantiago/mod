{
    "development": {
            "appName": "MOD",
            "dbAddress": "postgres://postgres:password@localhost:5432/mydb",
            "localAddress": "localhost",
            "appPort": 8080,
            "appProtocol": "http"
    },

    "production": {
            "appName": "MOD",
            "dbAddress": "postgres://postgres:password@localhost:5432/mydb",
            "localAddress": "localhost",
            "appPort": 8080,
            "appProtocol": "http"
    }
}
